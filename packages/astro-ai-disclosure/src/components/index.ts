// Barrel for the `.astro` components, which ship as source rather than being
// compiled into `dist/`. `AIImage` is added in step 1.3 and `AIPicture` in
// step 1.4; until then this module intentionally exports nothing.
export {};
